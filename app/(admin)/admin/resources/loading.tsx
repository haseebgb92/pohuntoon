import { LoadingState } from "@/components/shared/loading-state";

export default function AdminResourcesLoadingPage() {
  return <LoadingState title="Loading resources" description="Preparing the admin resource workspace." />;
}
