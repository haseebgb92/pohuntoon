import { EmptyState } from "@/components/shared/empty-state";

export default function LearningPage() {
  return (
    <EmptyState
      title="Learning is ready for feature work"
      description="Course catalogs, learning paths, and completion history will plug into this route next."
    />
  );
}
